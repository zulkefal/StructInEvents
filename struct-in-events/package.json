{
  "name": "struct-in-events",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ struct-in-events",
    "create-local": "graph create --node http://localhost:8020/ struct-in-events",
    "remove-local": "graph remove --node http://localhost:8020/ struct-in-events",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 struct-in-events",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.91.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
